#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age=18;
    if(age>=18)
        printf("Candidate is Eligible to Vote");
    else
        printf("Candidate is Ineligible to Vote");
    return 0;
}
