#include <stdio.h>
#include <stdlib.h>

int main()
{
    int s,e,a;
    printf("Enter the starting range : ");
    scanf("%d", &s);
    printf("Enter the ending range : ");
    scanf("%d", &e);
    for(a=s;a<s;a++)
        if(a%4==0)&&(a%100==0)
    {
        printf("Leap year %d \n", a);
    }
    return 0;
}
